#include <stdio.h>

/**
 * main -Entry point
 * program that prints name of file compile followed by new line.
 * Return: Always 0.
 */

int main(void)
{
	printf("File name: %s\n", __FILE__);
	return (0);
}
