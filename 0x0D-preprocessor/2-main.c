#include <stdio.h>
#define NAME __FILE__
/**
 * main -Entry point
 * program that prints name of file compile followed by new line.
 * Return: Always 0.
 */

int main(void)
{
	printf("File name: %s\n", NAME);
	return (0);
}
